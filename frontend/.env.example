# Copy to `.env.local` and edit if your backend is somewhere other than
# http://localhost:8000. Next.js only exposes vars prefixed with NEXT_PUBLIC_
# to the browser.

NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
